query characters($page: Int!) {
    characters(page: $page) {
        info {
            count
            prev
            next
        }
        results {
            id
            name
            status
            image
            species
            gender
        }
    }
}
