import { colors } from '../assets/colors';

export const largeTitleOptions = {
  headerLargeTitle: true,
  headerStyle: {
    backgroundColor: colors.background.gradient.top,
  },
  headerTitleStyle: {
    color: colors.text.navigationBar,
  },
};
